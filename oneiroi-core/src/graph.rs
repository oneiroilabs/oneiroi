// A Graph becomes a new node so that node can also be cached for that matter.

pub struct SingletonEvaluator {
    //all graphs
    // deduplicating _unique_ grpahs somehow.
    // evaluating the same graphs together to SIMD

    // maybe workgroup shared memory.
}
