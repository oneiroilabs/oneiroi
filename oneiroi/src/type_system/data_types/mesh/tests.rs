use insta::assert_yaml_snapshot;

use crate::nodes::Node;

#[test]
fn compute_cylinder() {
    //assert_yaml_snapshot!(CylinderV1::default().compute(None,))
}

#[test]
fn compute_box() {
    //assert_yaml_snapshot!(BoxV1::default().compute(None))
}
/*
#[test]
fn bounding_box() {
    let test = BoxV1::default();
    test.compute(None, input_sockets, context)
}
 */
